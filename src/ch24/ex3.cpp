// Expert level only (this cannot be done with the facilities described in this
// book): Write an apply(f,a) that can take a void (T&) , a T (const T&) , and
// their function object equivalents. Hint: Boost::bind

// Skipping for now.
#include <iostream>
#include <string>
#include <fstream>

int main()
{
  try {
    return 0;
  }
  catch (const std::exception& e) {
    std::cerr << e.what() << '\n';
    return 1;
  }
}
